/// Bundle identifiers for system applications used in UI tests.
enum AppBundleIDs {
	
	/// The bundle identifier for the Springboard app, used for interacting with system alerts.
	static let springboard = "com.apple.springboard"
	
	/// The bundle identifier for the Safari app, used for testing interactions within Safari.
	static let safari = "com.apple.mobilesafari"
	
	/// The bundle identifier for the Settings app, used for managing system settings during tests.
	static let settings = "com.apple.Preferences"
}
